PYTHON

Dev Environments
	Sublime Text
	Jupyter
	pip
		- install all packages with bash

Data types and assignment
	int, num
	string
		- string operations (concat, split, substring)
	list (iterable)
		- append
		- pop
	dict (iterable)
		- dict.keys()
		- dict.values()
	Big O(n) notation

Flow control
	if, elif, else
		- implicit Falseness of None, empty lists, etc...
		- compound statements
	for
		- using integer ranges
		- enumerate()
		- using lists and dicts (and pandas Series)
	while

Functions


PANDAS
